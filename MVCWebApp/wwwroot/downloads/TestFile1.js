function logPoint(point) {
    var result = "x = ".concat(point.x.toString(), ", y = ", point.y.toString());
    return result;
    //return "x = " + point.x.toString() + ", y = " + point.y.toString();
}
function logName(x) {
    return "Hello there, how the heck are things" + x.name + "?";
}
